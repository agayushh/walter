# May 2026

## 2026-05-01 10:56 — sidebar thread titles

Worked on sidebar thread titles. Kept the row height stable so the list does not jump when the label resolves. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-01 11:08 — sidebar scroll position

Worked on sidebar scroll position. Moved the update behind the existing layout so scroll position stays where the user left it. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-01 12:22 — new chat entry point

Worked on new chat entry point. Tightened the spacing without changing the click target. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-01 13:35 — message bubble spacing

Worked on message bubble spacing. Left a short comment in the notes for the case that only shows up on long threads. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-01 19:22 — composer height while typing

Worked on composer height while typing. Checked the narrow layout and the desktop layout against the same thread. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-02 12:56 — streaming token paint

Worked on streaming token paint. Repeated the flow after a refresh to confirm the state still restores. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-02 19:19 — stop-generation control

Worked on stop-generation control. Adjusted the copy so a failure tells the user what to do next. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-03 10:28 — model switcher label

Worked on model switcher label. Avoided a second request when the first one is still open. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-03 15:11 — empty chat placeholder

Worked on empty chat placeholder. Trimmed work that ran again on every token. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-03 20:50 — library document list

Worked on library document list. Made the empty state explicit instead of rendering a blank panel. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-04 10:28 — quiz generation prompt

Worked on quiz generation prompt. Preserved the draft if navigation happens mid-edit. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-04 12:30 — interview session header

Worked on interview session header. Stopped the control from submitting while the field is empty. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-04 15:11 — arena side-by-side layout

Worked on arena side-by-side layout. Matched the loading row to the final row so the handoff is quiet. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-04 17:41 — file upload progress

Worked on file upload progress. Kept auth failures on the sign-in path instead of a generic error page. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-04 20:56 — pdf text extraction

Worked on pdf text extraction. Scoped the read to the signed-in user before returning rows. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-05 11:23 — memory retrieval snippet

Worked on memory retrieval snippet. Confirmed the keyboard path and the pointer path land on the same handler. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-05 12:14 — auth redirect after sign-in

Worked on auth redirect after sign-in. Kept the row height stable so the list does not jump when the label resolves. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-05 15:46 — chat route loading state

Worked on chat route loading state. Moved the update behind the existing layout so scroll position stays where the user left it. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-05 17:10 — top bar model name

Worked on top bar model name. Tightened the spacing without changing the click target. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-06 12:46 — mobile sidebar drawer

Worked on mobile sidebar drawer. Left a short comment in the notes for the case that only shows up on long threads. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-06 19:44 — long message line wrap

Worked on long message line wrap. Checked the narrow layout and the desktop layout against the same thread. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-06 20:15 — code block overflow

Worked on code block overflow. Repeated the flow after a refresh to confirm the state still restores. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-06 21:57 — citation excerpt length

Worked on citation excerpt length. Adjusted the copy so a failure tells the user what to do next. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-07 10:52 — document delete confirm

Worked on document delete confirm. Avoided a second request when the first one is still open. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-07 13:31 — chat rename inline edit

Worked on chat rename inline edit. Trimmed work that ran again on every token. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-07 15:16 — history list date grouping

Worked on history list date grouping. Made the empty state explicit instead of rendering a blank panel. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-07 20:11 — error toast on failed send

Worked on error toast on failed send. Preserved the draft if navigation happens mid-edit. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-08 14:42 — retry after stream drop

Worked on retry after stream drop. Stopped the control from submitting while the field is empty. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-08 16:08 — keyboard submit shortcut

Worked on keyboard submit shortcut. Matched the loading row to the final row so the handoff is quiet. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-08 18:47 — paste of multi-line prompts

Worked on paste of multi-line prompts. Kept auth failures on the sign-in path instead of a generic error page. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-08 21:52 — dark surface contrast

Worked on dark surface contrast. Scoped the read to the signed-in user before returning rows. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-09 17:03 — focus ring on the composer

Worked on focus ring on the composer. Confirmed the keyboard path and the pointer path land on the same handler. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-09 18:15 — unread thread marker

Worked on unread thread marker. Kept the row height stable so the list does not jump when the label resolves. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-10 10:38 — archive chat action

Worked on archive chat action. Moved the update behind the existing layout so scroll position stays where the user left it. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-10 14:21 — search within a thread

Worked on search within a thread. Tightened the spacing without changing the click target. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-11 10:38 — attachment chip removal

Worked on attachment chip removal. Left a short comment in the notes for the case that only shows up on long threads. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-11 11:19 — image-less file cards

Worked on image-less file cards. Checked the narrow layout and the desktop layout against the same thread. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-11 12:51 — schema migration note

Worked on schema migration note. Repeated the flow after a refresh to confirm the state still restores. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-11 16:33 — drizzle config path

Worked on drizzle config path. Adjusted the copy so a failure tells the user what to do next. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-12 13:15 — middleware auth matcher

Worked on middleware auth matcher. Avoided a second request when the first one is still open. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-12 18:13 — sign-up field validation

Worked on sign-up field validation. Trimmed work that ran again on every token. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-12 19:41 — sso callback return path

Worked on sso callback return path. Made the empty state explicit instead of rendering a blank panel. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-12 21:16 — provider error mapping

Worked on provider error mapping. Preserved the draft if navigation happens mid-edit. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-13 15:47 — openai fallback message

Worked on openai fallback message. Stopped the control from submitting while the field is empty. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-13 16:34 — gemini timeout copy

Worked on gemini timeout copy. Matched the loading row to the final row so the handoff is quiet. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-13 18:21 — context trim before send

Worked on context trim before send. Kept auth failures on the sign-in path instead of a generic error page. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-13 21:51 — system prompt for quizzes

Worked on system prompt for quizzes. Scoped the read to the signed-in user before returning rows. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-14 11:34 — interview role presets

Worked on interview role presets. Confirmed the keyboard path and the pointer path land on the same handler. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-14 15:24 — arena judge prompt

Worked on arena judge prompt. Kept the row height stable so the list does not jump when the label resolves. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-14 16:31 — library empty state

Worked on library empty state. Moved the update behind the existing layout so scroll position stays where the user left it. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-14 17:49 — chat title generator

Worked on chat title generator. Tightened the spacing without changing the click target. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-14 20:16 — random title collisions

Worked on random title collisions. Left a short comment in the notes for the case that only shows up on long threads. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-15 13:29 — message timestamp format

Worked on message timestamp format. Checked the narrow layout and the desktop layout against the same thread. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-15 17:20 — copy message button

Worked on copy message button. Repeated the flow after a refresh to confirm the state still restores. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-15 20:10 — scroll-to-bottom button

Worked on scroll-to-bottom button. Adjusted the copy so a failure tells the user what to do next. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-15 21:48 — sticky composer on mobile

Worked on sticky composer on mobile. Avoided a second request when the first one is still open. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-16 17:18 — safe area under the keyboard

Worked on safe area under the keyboard. Trimmed work that ran again on every token. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-16 18:55 — reduced motion for streams

Worked on reduced motion for streams. Made the empty state explicit instead of rendering a blank panel. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-17 19:08 — loading skeleton rows

Worked on loading skeleton rows. Preserved the draft if navigation happens mid-edit. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-17 20:51 — deleted chat deep link

Worked on deleted chat deep link. Stopped the control from submitting while the field is empty. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-17 21:38 — shared layout sidebar width

Worked on shared layout sidebar width. Matched the loading row to the final row so the handoff is quiet. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-18 13:19 — readme setup steps

Worked on readme setup steps. Kept auth failures on the sign-in path instead of a generic error page. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-18 14:39 — env example keys

Worked on env example keys. Scoped the read to the signed-in user before returning rows. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-18 16:13 — upload size guard

Worked on upload size guard. Confirmed the keyboard path and the pointer path land on the same handler. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-18 21:55 — unsupported file type copy

Worked on unsupported file type copy. Kept the row height stable so the list does not jump when the label resolves. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-19 11:18 — partial stream recovery

Worked on partial stream recovery. Moved the update behind the existing layout so scroll position stays where the user left it. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-19 13:29 — duplicate send guard

Worked on duplicate send guard. Tightened the spacing without changing the click target. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-19 17:47 — optimistic user bubble

Worked on optimistic user bubble. Left a short comment in the notes for the case that only shows up on long threads. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-19 18:27 — assistant placeholder row

Worked on assistant placeholder row. Checked the narrow layout and the desktop layout against the same thread. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-19 19:12 — markdown link styling

Worked on markdown link styling. Repeated the flow after a refresh to confirm the state still restores. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-20 10:11 — list indentation in replies

Worked on list indentation in replies. Adjusted the copy so a failure tells the user what to do next. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-20 11:51 — table overflow in bubbles

Worked on table overflow in bubbles. Avoided a second request when the first one is still open. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-20 12:50 — selection color in dark mode

Worked on selection color in dark mode. Trimmed work that ran again on every token. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-20 13:41 — sidebar collapse animation

Worked on sidebar collapse animation. Made the empty state explicit instead of rendering a blank panel. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-20 21:12 — thread hover actions

Worked on thread hover actions. Preserved the draft if navigation happens mid-edit. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-21 10:10 — pin chat control

Worked on pin chat control. Stopped the control from submitting while the field is empty. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-21 12:25 — logout return path

Worked on logout return path. Matched the loading row to the final row so the handoff is quiet. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-21 16:41 — session refresh on 401

Worked on session refresh on 401. Kept auth failures on the sign-in path instead of a generic error page. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-21 21:30 — chat id in the url

Worked on chat id in the url. Scoped the read to the signed-in user before returning rows. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-22 10:08 — back navigation from a thread

Worked on back navigation from a thread. Confirmed the keyboard path and the pointer path land on the same handler. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-22 11:33 — browser tab title

Worked on browser tab title. Kept the row height stable so the list does not jump when the label resolves. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-22 12:19 — favicon contrast

Worked on favicon contrast. Moved the update behind the existing layout so scroll position stays where the user left it. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-22 19:25 — form submit pending state

Worked on form submit pending state. Tightened the spacing without changing the click target. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-23 11:05 — disabled send when empty

Worked on disabled send when empty. Left a short comment in the notes for the case that only shows up on long threads. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-23 13:10 — shift-enter newline

Worked on shift-enter newline. Checked the narrow layout and the desktop layout against the same thread. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-24 16:36 — ime composition enter key

Worked on ime composition enter key. Repeated the flow after a refresh to confirm the state still restores. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-24 20:27 — drag and drop highlight

Worked on drag and drop highlight. Adjusted the copy so a failure tells the user what to do next. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-25 11:23 — multi-file drop order

Worked on multi-file drop order. Avoided a second request when the first one is still open. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-25 14:17 — upload cancel button

Worked on upload cancel button. Trimmed work that ran again on every token. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-25 15:49 — extracted text preview

Worked on extracted text preview. Made the empty state explicit instead of rendering a blank panel. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-25 18:08 — quiz answer reveal

Worked on quiz answer reveal. Preserved the draft if navigation happens mid-edit. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-26 10:10 — interview timer label

Worked on interview timer label. Stopped the control from submitting while the field is empty. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-26 14:25 — arena vote buttons

Worked on arena vote buttons. Matched the loading row to the final row so the handoff is quiet. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-26 19:54 — judge latency note

Worked on judge latency note. Kept auth failures on the sign-in path instead of a generic error page. Verified the change against a fresh thread and an existing thread before leaving it.

## 2026-05-26 21:05 — memory panel empty copy

Worked on memory panel empty copy. Scoped the read to the signed-in user before returning rows. Verified the change against a fresh thread and an existing thread before leaving it.

